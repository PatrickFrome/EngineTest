# Recurring resistant-selector example

Two synthetic living runs preserve one source locator but route it through two rival structural unitizations under residual/revision pressure. The discovery engine emits one review case and a deliberately non-promotable delta seed.

Run:

```bash
node studio/studio.mjs discover:resistance fixtures/discovery/recurring-rival-routing
```
