# Destruktion portable-agent contract

Read `00_START_HERE.md`, `PORTABLE_CHAT_PROJECT.md` and `PORTABLE_PROJECT.json` before acting on this repository.

- Treat the archive as a portable philosophical-analysis project, not merely a codebase.
- Detect runtime capability. Use the real CLI when Node.js and file operations are available; otherwise use `portable/DOCUMENT_ONLY_PROTOCOL.md` and label the result `DOCUMENT_ONLY_LANGUAGE_MODEL_EXECUTION`.
- Never describe document-only reasoning as engine execution.
- ETY-0.2 is mandatory for every central concept. Its philosophical significance is not mandatory.
- Keep `CORE 4.0.0-alpha.1` frozen. Experimental living and portable layers remain external overlays.
- Prefer `PHILOSOPHICAL_FIELD_NOTE` as the living result and `FINAL_ANALYTICS` as the run-bound adjudicative result.
- Do not invent quotations, page locators, historical etymologies, source resolution or independent validation.
- A generative move without a traceable GG1–GG7 gain must be skipped or retired.
- End substantive analyses with a self-destruction of the result, typed remainder and reopening condition.
- Preserve user files and unrelated work. Outputs belong in new directories.


- Source resistance is asymmetric: an explicit project thesis may demand adjudication, but only source-backed registry blindness may activate GX7/operator mutation.
- A source-forced operator delta is experimental, reversible and non-Core. Keep rival unitizations live and preserve rollback plus cross-corpus regression.
- Do not promote `RELATION_FIRST`, `CO_CONSTITUTIVE` or any other representation mode to ontology merely because it improves one corpus. `UNRESOLVED_ONTOLOGY` must remain available.
- ETY stress can force an ETY card, including a null result, but cannot by itself force a topic or operator mutation.
- In 0.10, a source-backed blind spot may emit `UNKNOWN_OPERATOR_FAMILY`; do not translate it back into a known relation profile merely to obtain closure.
- Route known and open-set candidates on micro-local argument windows. Preserve `ABSTAIN_LOCAL` and local rival states.
- `ADD_OPERATOR` may modify only a candidate registry. Never mutate the baseline registry or frozen CORE in place.
- Treat lexical recurrence/co-occurrence as a provisional source signature, not semantic validation; quotation, negation, modality and attribution remain explicit 0.10 limits.
