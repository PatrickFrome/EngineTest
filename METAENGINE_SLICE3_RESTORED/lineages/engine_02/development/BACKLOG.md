# Development backlog

## DONE in Studio 0.2 — RESISTANT-SOURCE / OPERATOR-MUTATION 1.0

Candidate-based mutation gate with same-source incompatible unitizations, explicit failure, reversible `SUSPEND | SPLIT | REVISE | ADD_CONDITION`, GG1–GG6, traceability and negative-test gates.

## DONE in Studio 0.3 — DECLARATIVE GENERATIVE GESTURES

- GX activation is represented as a bounded boolean AST;
- GX emission is represented as a generic emission program;
- new/split gestures compile without gesture-ID-specific JavaScript;
- generative-gesture mutations can receive `FULL` runtime reachability;
- sufficient-openness semantics are role-based rather than GX-name-based;
- A/B/C comparator runs frozen baseline → declarative baseline → declarative mutant under one refinery/seed;
- structural growth is separated from evidence of a new distinction.

## P0 — RESISTANT-SOURCE DISCOVERY

Current delta creation still starts with a human naming the failure. The next engine should detect repeated operator strain and produce a **delta hypothesis**, not an automatic promotion.

Minimum contract:

- collect unresolved/reopened nodes across runs;
- detect repeated rival unitizations or traceable residual patterns;
- identify which operator/gesture is repeatedly implicated;
- generate a bounded `operator_delta` draft with source selectors and failure evidence;
- never self-promote; require the existing mutation gate and review;
- distinguish source resistance from parser/coverage failure and from simple low evidence.

## P1 — RHETORICAL COMPOSER

Writing form should respond to graph structure while keeping operator provenance and source limits visible.

## P1 — RESISTANT LENS DISCOVERY

Topic lenses should register material that fits no existing lens instead of silently assigning the nearest category.

## P2 — ETY SOURCE ADAPTERS

Pluggable historical dictionary/corpus adapters with separate status for historical linguistics, authorial wordplay and local usage.

## P2 — EXTERNAL BENCHMARK

Licensed corpus suite, independent coders and domain-native comparator. Promotion claims remain blocked until then.

## After Studio 0.4

- Build a source-bound review bridge that materializes the exact cited span beside a discovery case without silently replacing it with generated paraphrase.
- Support controlled human-authored rival unitization cards attached to one locator.
- Add discovery-to-delta authoring assistance that remains non-promotional until executable semantic tests exist.
- Test recurrence across a real multi-seed / multi-registry Heidegger corpus run after AJV installation.
